# Redmine Configuration
REDMINE_URL=https://dev.zigexn.vn

# HTTP Basic Authentication (cho nginx/apache protection layer)
# Nếu website được bảo vệ bởi HTTP Basic Auth, cần thiết lập 2 biến này
HTTP_USERNAME=your_http_username
HTTP_PASSWORD=your_http_password

# Redmine API Authentication
# Chỉ cần MỘT trong hai phương thức sau:

# Phương thức 1: API Key (Khuyến nghị khi có HTTP Basic Auth)
REDMINE_API_KEY=your_redmine_api_key_here

# Phương thức 2: Username/Password (chỉ dùng khi KHÔNG có HTTP Basic Auth)
# REDMINE_USERNAME=your_redmine_username
# REDMINE_PASSWORD=your_redmine_password

# Hướng dẫn lấy Redmine API Key:
# 1. Đăng nhập vào Redmine
# 2. Vào My account (góc trên bên phải)
# 3. Click vào tab "API access key" hoặc "Access keys"
# 4. Click "Show" để xem API key hoặc "Reset" để tạo mới
# 5. Copy API key và paste vào REDMINE_API_KEY ở trên

# GitHub Configuration (optional - for crawling PR/Issue info)
GITHUB_TOKEN=your_github_personal_access_token

# Hướng dẫn tạo GitHub Personal Access Token:
# 1. Đăng nhập vào GitHub
# 2. Vào Settings > Developer settings > Personal access tokens > Tokens (classic)
# 3. Click "Generate new token" > "Generate new token (classic)"
# 4. Đặt tên cho token, chọn scope "repo" (Full control of private repositories)
# 5. Click "Generate token" và copy token
# 6. Paste vào GITHUB_TOKEN ở trên
